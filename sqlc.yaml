version: "2"

sql:
  - engine: postgresql
    schema: db/migrations/000001_create_rooms.up.sql
    queries: db/queries
    gen:
      go:
        package: dbgen
        out: internal/postgres/dbgen
        sql_package: pgx/v5
        emit_db_tags: true
        emit_empty_slices: true
        emit_pointers_for_null_types: true
